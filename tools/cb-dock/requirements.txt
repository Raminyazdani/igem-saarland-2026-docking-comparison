# Python dependencies for CB-Dock3 (tool/cb-dock) — MANAGED BY Ramin & Elnaz (coordinators).
#
# Teams: do NOT edit this file. If your tool needs a package, ask a coordinator to add it here.
# Coordinators fill in the real packages + pinned versions; then teams install with:
#     pip install -r tools/<tool>/requirements.txt
#
# (Some tools run in their own environment — Schrodinger/CCDC/BIOVIA — or are web-based and need
#  nothing here. Leave this commented/empty until the tool's real deps are decided.)
#
# TODO (Ramin/Elnaz): add + pin this tool's docking dependencies below.
# CB-Dock3 is a web server. Usually no local deps.
# requests   # only if you script uploads/downloads

# Used by scripts/download_cb_dock_results.py for browser automation and downloads.
playwright==1.62.0
